<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smhrd.db.SafePlaceMapper">

    <!-- 모든 안전 장소를 조회하는 쿼리 -->
    <select id="selectAllSafePlaces" resultType="com.smhrd.model.SafePlaceDTO">
        SELECT * FROM SAFE_PLACES
    </select>

</mapper>